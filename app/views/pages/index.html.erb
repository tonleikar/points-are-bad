<div>
  <h1 class="font-bold text-4xl">Pages#index</h1>
  <ul>
    <% for match in @matches do %>
      <li><%= match.fetch('homeTeam')['name'] %> v <%= match.fetch('awayTeam')['name'] %></li>
    <% end %>
  </ul>
</div>
